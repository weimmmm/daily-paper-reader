---
title: "ViTL: Temporal Logic-Guided Zero-Shot Natural Language Navigation via Vision-Language Models"
title_zh: "ViTL: 基于视觉-语言模型的时间逻辑引导零样本自然语言导航"
authors: "Kaier Liang, Hengde Dai, Cristian-Ioan Vasile"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30696v1"
tags: ["query:embodied-rl"]
score: 9.0
evidence: 基于时态逻辑的视觉语言导航
tldr: 现有零样本导航方法仅支持单目标任务，无法处理含时序逻辑约束的多目标命令。ViTL框架通过LLM将自然语言命令编译为LTL公式并转为DFA，协调多通道价值图，同时引入方向评分提取VLM的每方向分数。在HM3D数据集上，ViTL首次实现了零样本长时域自然语言导航，方向评分还提升了单目标导航的精度和效率。该工作为机器人执行复杂时序命令提供了新范式，拓展了VLM在零样本任务中的应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30696v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1659, \"height\": 1017, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 776, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30696v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 379, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 906, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 904, \"height\": 336, \"label\": \"Table\"}]"
motivation: 现有零样本导航无法处理含时序与逻辑约束的多目标自然语言命令，如“先清洁椅子或沙发，然后打开电视”。
method: 用LLM将命令编译为LTL公式并转为DFA，协调多通道价值图；引入方向评分从VLM提取每方向分数实现动态重规划。
result: 在HM3D数据集上，ViTL成功完成零样本长时域导航任务，方向评分使单目标导航成功率和效率均超基线。
conclusion: 首个处理时序约束的零样本自然语言导航框架，显著扩展了VLM在机器人长时域任务中的能力。
---

## 摘要
使机器人能够遵循自然语言指令完成零样本长时程任务仍然具有挑战性。这需要从自然语言指令中提取隐含的时间与逻辑约束，并据此执行多个子任务。最近的零样本目标导航方法使用视觉-语言模型引导未知环境中的基于前沿的探索，但仅限于单目标任务。诸如“清洁椅子或沙发，然后打开电视”的现实指令需要在时间约束顺序下导航至多个目标，现有零样本系统无法处理。我们提出ViTL框架，在任务层和导航层分别解决这一问题。在任务层，我们使用大型语言模型将自然语言指令编译为线性时序逻辑公式，再将其转换为确定性有限自动机，用于协调多通道价值图并在检测到新目标时触发动态重规划。在导航层，我们引入方向评分：不生成整个视野内方向无关的价值，而是在观测图像上标记前沿方向，并从视觉-语言模型中提取每个方向的方向评分。在Habitat-Matterport 3D上的实验表明，完整框架能够实现具有时间约束的零样本长时程自然语言导航任务，并且方向评分相比于基线提高了单目标导航的准确性与效率。

## Abstract
Enabling robots to follow natural language commands to complete zero-shot long-horizon tasks remains challenging. It requires extracting implicit temporal and logical constraints from natural language commands and executing multiple sub-tasks accordingly. Recent zero-shot object navigation methods use vision-language models (VLMs) to guide frontier-based exploration in unknown environments, but they are limited to single-target tasks. Real-world commands such as "Clean either the chair or the couch, then turn on the tv." require navigating to multiple targets in a temporally constrained order, which no existing zero-shot system can handle. We present ViTL, a framework that addresses this gap at two levels. At the task level, we use a large language model (LLM) to compile natural language commands into Linear Temporal Logic (LTL) formulas, which are then converted into Deterministic Finite Automata~(DFA) that coordinate multi-channel value maps and trigger dynamic replanning when new objects are detected. At the navigation level, we introduce directional score: rather than producing a direction-agnostic value across the entire field of view, we label frontier directions on the observation image and extract per-direction scores from the VLM. Experiments on Habitat-Matterport 3D (HM3D) show that the full framework enables zero-shot long-horizon completion of natural language navigation tasks with temporal constraints, and that directional score improves single-target navigation accuracy and efficiency over the baseline.

---

## 论文详细总结（自动生成）

# 论文总结：ViTL：基于视觉-语言模型的时间逻辑引导零样本自然语言导航

## 1. 核心问题与整体含义
- **研究动机**：现有零样本目标导航方法（如CoW、VLFM、InstructNav）仅限于单目标任务，无法处理现实世界中含有时序和逻辑约束的多目标自然语言命令（例如：“清洁椅子或沙发，然后打开电视”）。这类命令要求机器人按特定顺序导航至多个目标，且存在析取（“椅子或沙发”）和顺序（“然后”）约束。
- **整体含义**：ViTL首次提出一个零样本导航框架，能够从自然语言命令中提取隐式时序约束，并完成长时程多目标导航任务，扩展了视觉-语言模型（VLM）在机器人任务规划中的应用。

## 2. 方法论
- **核心思想**：将任务分解为高层规划（时序约束满足）和低层导航（单目标探索）。高层用LLM将自然语言编译为线性时序逻辑（LTL）公式，再转为确定性有限自动机（DFA），通过图搜索动态选择当前目标子任务；低层引入方向评分（directional score），让VLM对单个前沿方向打分，构建平滑价值图引导探索。
- **关键技术细节**：
  - **任务层规划**：
    - 通过单次LLM调用将自然语言命令编译为LTL公式（如`◊((chair ∨ couch) ∧ ◊tv)`，其中`◊`表示“最终”）。
    - LTL公式编译为DFA，DFA状态根据是否到达目标推进，边权重根据目标是否被检测到（`w_det ≪ 1`）、是否失败（`w_fail ≫ 1`）或未检测（权重1.0）动态调整。
    - 从当前DFA状态到接受状态的最短加权路径决定下一个导航目标，当检测/失败/到达事件发生时触发重规划。
    - 维护多通道价值图（每目标类别一个通道），所有通道并行更新，避免目标切换时丢失探索信息。
  - **导航层执行器**：
    - **方向评分**：在RGB图像上标注每个前沿方向（字母标签），构造多项选择提示让VLM输出每个方向的概率（softmax over next-token logits）。额外选项“explore elsewhere”作为校准。
    - 场景相关性评分：VLM回答“该视图是否可能通向目标？”得到`S_curr`。
    - 每个前沿的当前价值：`V_curr(f_i) = S_curr × P(f_i|I_t, c_k)`。
    - **时序融合**：通过场景加权平均融合新旧观察，更高信息量的观察贡献更大。
    - **空间平滑**：在FOV锥内进行角度线性插值，使离散的前沿得分变为连续价值图。
    - **前沿选择**：在价值图上选取半径r内最大值对应的前沿作为导航路点，由预训练PointNav策略驱动。
- **算法流程**（文字说明）：
  1. 编译命令得到DFA，初始化DFA状态s=s0，检测集D、到达集R、失败集B为空，并初始化多通道价值图M。
  2. 通过最短加权路径选择当前目标ck。
  3. 循环（未到达接受态且未超步数）：
     - 检测当前观测中的目标类别，若发现新目标则更新D并重规划。
     - 更新所有价值图通道（方向评分+融合平滑）。
     - 若当前目标已被检测，则直接导航至其位置；否则通过价值图选择最高分前沿进行探索。
     - 若到达目标，则推进DFA状态并更新R；若失败则加入B并重规划。
  4. 返回是否成功到达接受状态。

## 3. 实验设计
- **数据集与场景**：
  - **单目标导航**：HM3D v2验证集（2000个episodes，20个场景，6个目标类别：chair, bed, potted plant, toilet, tv monitor, couch）。
  - **任务层规划**：自建基准2000个自然语言导航任务，每任务对应一个LTL公式（模板最大含6个目标、析取和until约束）。通过模拟单目标执行器（概率模型`p_obs`从0.1到1.0扫描）评估规划器。
  - **全系统评估**：从HM3D中选取两个单目标成功率达~90%的场景（`ziup5kvtCCR`和`svBbv1Pavdk`），用于减少导航层噪声。
- **对比方法**：
  - **单目标**：CoW、ESC、L3MVN、VLFM、InstructNav（GPT4+GPT4V/LLaVA-34B）、WMNav（Gemini 1.5 Flash/Pro）、GAMap、SG-Nav-GPT。
  - **任务层规划**：LLM-planner（使用GPT-5.2，每次决策点重新查询LLM选择目标）。
  - **全系统**：与LLM-planner比较在50个相互完成的任务上的路径长度。
- **评价指标**：Success Rate (SR)、Success weighted by Path Length (SPL)、任务完成率、探索尝试次数、路径长度。

## 4. 资源与算力
- **文中明确提及**：
  - VLM：LLaVA-NeXT-34B，加载8-bit量化。
  - 目标检测：YOLOv7-E6E。
  - 底层PointNav策略：预训练深度模型（来自VLFM）。
  - 未说明使用的GPU型号、数量、训练时长或推理总耗时。仅提到使用8-bit量化减小内存占用。
- **结论**：论文未提供详细的算力需求分析，重点在方法设计，忽略计算成本。

## 5. 实验数量与充分性
- **实验数量**：
  - 单目标：2000 episodes，与7种方法对比，结果报告在Table I。
  - 任务层：2000个任务 × 10个`p_obs`值 = 20,000次仿真（但实际为扫描参数，每个`p_obs`下可能运行多次？文中图4展示10个点）。
  - 全系统：2个场景，50个相互完成的任务。
  - 消融实验：无显式消融（但方向评分与VLFM的对比可视为对导航层设计的消融）。
- **充分性**：
  - 单目标实验大规模、多方法对比，客观公平（使用相同评估协议）；但仅报告平均结果，未区分目标类别或场景难度。
  - 任务层规划实验设计巧妙（概率模型），但未与真实导航结合，仅反映规划器理想性能。
  - 全系统评估样本量小（50 episodes），仅在高成功率场景下测试，泛化性不足。
  - 缺少与多目标非时序基线（如随机顺序或贪心策略）的对比。

## 6. 主要结论与发现
- ViTL任务层规划器的NL-to-LTL翻译准确率达95.3%（1906/2000正确），给定正确翻译后保证100%任务完成；而LLM-planner（GPT-5.2）仅88.3%完成率，且常违反约束或提前终止。
- 方向评分在单目标导航中达到54.1% SR和30.9 SPL，与基于大闭源模型的VLFM（52.4%, 30.8）持平，优于其他开源方法。
- 全系统评估中，ViTL在50个相互完成的任务上平均路径长度25.45 m，比LLM-planner的30.05 m减少15.31%，验证了动态重规划的路径效率。

## 7. 优点
- **创新性**：首次将LTL/DFA与VLM结合，解决零样本时序约束多目标导航问题，填补空白。
- **方法论**：任务层采用一次LLM调用+图搜索，避免反复查询LLM的开销和推理错误；方向评分利用VLM的空间感知能力，生成精细价值图。
- **实验结果**：单目标性能与最先进方法相当，任务规划大幅优于LLM基线，全系统显示实际路径节省。
- **可复现性**：使用开源模型（LLaVA-34B、YOLOv7）和标准仿真器（Habitat、HM3D），代码和设置易于复现。

## 8. 不足与局限
- **任务简化**：所有动作动词被简化为“到达对象”，忽略实际交互（如清洁、打开）。未考虑子任务内部的操作序列。
- **实验覆盖**：
  - 全系统仅在高成功率场景上评估（50 episodes），低成功率场景下噪声大，规划器优势可能被掩盖。
  - 任务层规划实验使用概率模型而非真实导航，未体现感知不确定性（如检测失败、假阳性）对规划的影响。
  - 未与多目标非时序基线（如按任意顺序逐个执行）比较。
- **LTL翻译依赖**：仍有4.7%的错误率，且LLM可能生成语法正确但语义不符的公式。文中提及微调小模型（T5-base）可接近完美，但未在本文使用。
- **计算开销**：VLM逐帧查询场景相关性和方向评分，可能产生高延迟；未分析推理速度或与实时需求的匹配。
- **泛化性**：仅测试HM3D室内场景，未在室外或不同领域验证；目标类别有限（6类），可能依赖VLM的预训练知识。

（完）
