---
title: "DART-VLN: Test-Time Memory Decay and Anti-Loop Regularization for Discrete Vision-Language Navigation"
title_zh: DART-VLN：面向离散视觉语言导航的测试时记忆衰减与反循环正则化
authors: "Shaoheng Zhang, Zhichen Li, Jie Mei"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01043v1"
tags: ["query:embodied-rl"]
score: 9.0
evidence: 离散视觉语言导航的测试时控制
tldr: 离散视觉语言导航中冻结backbone在测试时因陈旧记忆和局部回溯导致性能下降。DART-VLN采用无训练测试时控制，通过记忆衰减抑制冗余证据，结合反循环正则化阻止立即回溯。在R2R和REVERIE上，该方法在保持主干不变下实现更短路径、更低耗时和更好导航效果，验证了测试时控制的实用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01043v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1825, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01043v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1807, \"height\": 532, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01043v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1862, \"height\": 619, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01043v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 582, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01043v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 884, \"height\": 434, \"label\": \"Table\"}]"
motivation: 冻结的离散VLN智能体测试时易受陈旧历史证据和无效局部回溯影响，导致导航不稳定。
method: 提出DART-VLN，包含测试时记忆衰减和反循环正则化，无训练干预。
result: R2R和REVERIE上，衰减+循环正则化实现更短轨迹、更低运行时和更好导航性能。
conclusion: 适度测试时控制可提升基于记忆的离散VLN的可靠性和效率，无需重训练。
---

## 摘要
基于记忆的离散视觉语言导航代理必须在部分可观测条件下行动，然而即使强大的冻结骨干网络在测试时也仍然脆弱。两个常见的失败模式是记忆读出时的过时历史证据以及动作选择期间的低效局部回溯。我们提出DART-VLN，一种无需训练的可用于离散VLN的测试时控制框架。DART-VLN结合了测试时记忆衰减（一种读侧记忆重加权规则，在不重写存储内容的情况下抑制过时和冗余证据）与反循环正则化（一种轻量的下一跳惩罚，在动作选择期间阻止立即反转）。该框架不引入任何新的可学习参数，并保持学习到的骨干网络不变。在R2R和REVERIE上的实验显示出一致的模式：仅使用衰减带来稳定的读侧增益，而衰减+反循环实现了最佳的整体质量-效率权衡，在关键设置中获得更短的轨迹、更低的运行时间以及改进的导航性能。行为分析进一步证实，在冻结骨干网络下，反循环正则化减少了局部回溯并提高了路径效率。总体而言，结果表明适度的测试时控制可以在无需重新训练的情况下使基于记忆的离散VLN更可靠、更高效。

## Abstract
Memory-based discrete vision-language navigation (VLN) agents must act under partial observability, yet even strong frozen backbones remain vulnerable at test time. Two common failure modes are stale historical evidence at memory readout and inefficient local backtracking during action selection. We present DART-VLN, a training-free test-time control framework for discrete VLN. DART-VLN combines Test-Time Memory Decay, a read-side memory reweighting rule that suppresses stale and redundant evidence without rewriting stored content, with Anti-Loop Regularization, a lightweight next-hop penalty that discourages immediate reversals during action selection. The framework introduces no new learnable parameters and leaves the learned backbone unchanged. Experiments on R2R and REVERIE show a consistent pattern: decay-only provides stable read-side gains, while decay+anti-loop achieves the best overall quality-efficiency trade-off, yielding shorter trajectories, lower runtime, and improved navigation performance in key settings. Behavioral analysis further confirms that anti-loop regularization reduces local backtracking and improves path efficiency under frozen backbones. Overall, the results show that modest test-time control can make memory-based discrete VLN more reliable and efficient without retraining.

---

## 论文详细总结（自动生成）

# DART-VLN：面向离散视觉语言导航的测试时记忆衰减与反循环正则化

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：基于记忆的离散视觉语言导航（VLN）代理在部分可观测环境下运行，即使使用强大的预训练骨干网络（冻结参数），在测试时仍然存在两个常见失败模式：①记忆读出时过时或重复的历史证据会污染当前决策；②动作选择时出现立即反转和短路径循环，导致路径变长、效率降低。
- **背景**：离散VLN在显式视角图上操作，现有方法依赖更强的预训练导航器、显式记忆/地图表示或更复杂的规划模块，但这些改进通常需要重新训练或架构重设计。本文探索**无需训练**的轻量级测试时控制，在不修改已训练骨干的前提下提升推理可靠性。
- **核心含义**：提出一个**无训练、无新增可学习参数**的测试时控制框架DART-VLN，通过**读侧记忆衰减**（抑制陈旧/冗余证据）和**动作侧反循环正则化**（惩罚立即回溯）来改善冻结骨干的推理行为，实现更好的质量-效率权衡。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 总览

DART-VLN嵌入在GridMM风格的离散VLN推理流水线中，在每一步干预两个环节：
- **记忆读出时**：对存储的记忆槽进行重加权（读侧衰减），不修改存储内容。
- **动作选择时**：对候选动作分数施加下一跳惩罚（反循环正则化），然后argmax选择动作。

### 2.2 测试时记忆衰减

- **动机**：随着轨迹增长，一些记忆槽变得过时、重复或只与当前决策弱相关，若以相同强度参与聚合会引入噪声。
- **槽元数据**：为每个记忆槽维护三个轻量变量：
  - 槽年龄 \(a_i = t - t_i^{last}\)（上次刷新后的步数）
  - 访问次数 \(c_i\)
  - 新颖度 \(n_i\)：通过指数移动平均平滑瞬时新颖度 \(\nu_i = \text{clip}(1 - \cos(f_i^{old}, f_i^{new}), 0, 1)\)，\(\rho=0.5\)。
- **读权重公式**：
  \[
  w_i = \text{clip}\left( e^{-\lambda a_i} \left(1 - \alpha \frac{c_i}{c_i+1}\right) (0.5 + 0.5 n_i),\; w_{\min}, w_{\max} \right)
  \]
  - 注意：衰减项 \(e^{-\lambda a_i}\)、重复抑制项 \((1 - \alpha \frac{c_i}{c_i+1})\)、新颖度偏好项 \((0.5+0.5n_i)\)。超参数 \(\lambda=0.12,\alpha=0.15,w_{\min}=0.35,w_{\max}=1.0\)。
- **仅读侧约束**：不修改存储内容，只在读出时加权。

### 2.3 反循环正则化

- **动机**：立即回溯（回到前一节点）和短循环是常见低效行为，会浪费步数并增加决策误差。
- **惩罚项**：对于候选目标视角 \(v\)，首先计算其**图下一跳** \(h(v)\)（从当前节点到 \(v\) 的第一条边），然后施加惩罚：
  \[
  p_t(v) = \beta_{\text{back}} \cdot \mathbb{I}[h(v)=v_{t-1}] + \beta_{\text{rev}} \cdot \mathbb{I}[\text{visit}(h(v)) \ge k]
  \]
  第一项惩罚立即回溯，第二项惩罚多次重访（\(k=2\)，\(\beta_{\text{rev}}=0.06\)，主效应由第一项主导 \(\beta_{\text{back}}=0.22\)）。调整后分数 \(s'_t(v) = s_t(v) - p_t(v)\)。
- **作用范围**：仅测试时应用，仅针对当前步骤的局部过渡，不修改stop头或骨干。

### 2.4 算法流程（文字描述）

1. 初始化轨迹和访问计数。
2. 每一步：编码指令和当前观测 → 计算读侧权重（式3） → 聚合加权记忆获得候选分数 → 对每个候选计算下一跳惩罚（式4） → 调整分数 → argmax选动作 → 若选中STOP则结束；否则执行转移，更新访问计数和槽元数据。

## 3. 实验设计

### 3.1 数据集与评测指标

- **R2R（Room-to-Room）**：在 `val unseen` 和 `test unseen` 上评测。指标：轨迹长度(TL)、导航误差(NE)、成功率(SR)、SPL、运行时。
- **REVERIE（Remote Embodied Visual Referring Expression）**：在 `val unseen` 上评测（官方test unseen提交系统已不可用）。指标：TL、OSR、SR、SPL、RGS、RGSPL、运行时。

### 3.2 基准方法与对比

- **基线**：GridMM（本文采用的主流离散VLN骨干）。
- **已发表方法**：VLN-BERT、AirBERT、SEvol、HOP、HAMT、TD-STP、DUET、BEVBert等（仅列出性能参考，运行时不可跨论文比较）。
- **本文变体**：
  - `gridMM`（基线）
  - `update-only`（探索性写侧更新，不使用读衰减）
  - `decay-only`（仅读侧记忆衰减）
  - `full-mode`（读衰减+写侧更新）
  - **`decay+anti-loop`（DART-VLN主设置）**

## 4. 资源与算力

- **文中未明确说明训练资源**（因为方法无需训练）。运行时测量在**同一硬件环境**（NVIDIA GeForce RTX 5070 Ti GPU）下进行，R2R和REVERIE的运行时均基于该GPU报告。未提及训练轮次、数据并行或预训练，因为所有变体使用相同的冻结checkpoint。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个基准数据集（R2R val/test unseen，REVERIE val unseen）共3个评估集。
  - 包含六个变体（baseline、update-only、decay-only、full-mode、decay+anti-loop、以及主设置）。
  - 额外行为分析表III（回溯率、平均步骤、平均TL）。
  - 提供了定性例子（图2）。
- **充分性评价**：
  - 消融充分：分离了衰减、反循环、写侧更新等组件的效果。
  - 公平性：所有变体共享同一基线checkpoint，只改变推理时开关，无重新训练。
  - 局限性：仅在一个骨干（GridMM）上验证，未扩展至其他记忆架构（如HAMT、DUET）。反循环正则化对最终端指标（如NE）并非单调改善，论文对此诚实说明。

## 6. 论文的主要结论与发现

- **主要发现**：
  1. 读侧记忆衰减（decay-only）在R2R和REVERIE上稳定提升NE、降低运行时，且不损害SR/SPL——验证了抑制陈旧证据的有效性。
  2. 添加反循环正则化（decay+anti-loop）进一步缩短轨迹、降低运行时、改善SPL/RGSPL，在质量-效率权衡上最优。例如，R2R val unseen上TL从13.27降至12.41，SPL从64升至66；REVERIE val unseen上SR从51.37升至52.34，RGSPL从24.56升至25.44，运行时降低约2/3。
  3. 更强干预（写侧更新）并不更好：update-only和full-mode表现不稳定，甚至劣于decay-only。**保守的测试时控制比激进重写更可靠**。
  4. 行为分析证实反循环正则化的直接效果是降低回溯率（R2R: 2.30%→2.01%；REVERIE: 8.43%→5.99%）。
- **总体结论**：在已有强大记忆型导航器基础上，适度的测试时控制（无重训练）即可改善推理行为，使离散VLN更可靠高效。

## 7. 优点

- **无需训练，无新增参数**：方法简单、即插即用，可应用于任何基于记忆的离散VLN推理管道。
- **解释性强**：记忆衰减的权重公式直观（年龄、重复、新颖度），反循环惩罚具有明确几何意义（下一跳）。
- **实验结果一致且透明**：在两个基准、多个变体上呈现一致趋势，并开放行为分析；诚实报告了反循环在某些指标上非单调提升。
- **轻量高效**：运行时显著降低（例如REVERIE上从4329s降至1498s），表明同时改进了效率和决策质量。

## 8. 不足与局限

- **骨干网络单一**：仅基于GridMM验证，未在HAMT、DUET等不同架构上测试，泛化性有待确认。
- **反循环正则化的边际增益有限**：虽然提升效率，但对最终导航成功率的提升较小（R2R上SR仅+1%~2%），且NE在部分设置下未改善。
- **未提供完全因果解释**：由于梯度不参与，无法严格证明衰减和反循环是性能提升的唯一原因；仅报告相关性。
- **写侧更新变体不稳定**：论文自身承认更激进的写侧更新不如保守读侧设计，但未深入分析原因（可能因冻结骨干的表示空间被扰）。
- **实验场景限制**：离散VLN，不覆盖连续环境或真实机器人部署；运行时度量仅在同一GPU上比较，不可跨论文。
- **未见多人、动态障碍等复杂情况**：当前室内静态数据集，更复杂的任务（如TEACh、ALFRED）未涉及。

（完）
