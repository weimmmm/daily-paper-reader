---
title: "Q-VGM: Q-Guided Value-Gradient Matching for Flow-Matching VLA Policies"
title_zh: Q-VGM：面向流匹配VLA策略的Q引导值梯度匹配
authors: "Ziqian Wang, Jiayu Sun, Xingjian Mao, Minqian Wang, Yao Mu"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08015v1"
tags: ["query:embodied-rl"]
score: 9.0
evidence: 面向具身智能体流匹配VLA策略的RL微调
tldr: "针对flow-matching VLA策略微调时难以利用Q函数梯度信息的问题，提出Q-VGM方法。通过VGG-Flow将值梯度转化为去噪时间上的梯度场，避免反向传播去噪链，并采用动作敏感的Cal-QL集成。在LIBERO上成功率从75.0%提升至92.5%，RoboTwin 2.0从76.4%提升至87.2%，真实机器人任务从40.0%提升至67.5%。实现了少样本初始化后利用自生成数据学习的有效范式，无需额外专家监督。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08015v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1508, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1504, \"height\": 852, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 556, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08015v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 437, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1121, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1078, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 750, \"height\": 457, \"label\": \"Table\"}]"
motivation: 解决flow-matching VLA策略微调中，直接反向传播值函数不稳定且似然不可得，难以利用梯度信息的问题。
method: Q-VGM利用VGG-Flow将值梯度转化为去噪时间上的梯度场，并结合动作敏感的Cal-QL集成，无需反向传播去噪链或计算似然。
result: "在LIBERO、RoboTwin 2.0和真实机器人任务上，成功率分别提升17.5%、10.8%和27.5%，超越同骨干网络基线。"
conclusion: 实现了从少样本初始化到基于经验学习的范式转变，高效提升策略性能且无需额外专家监督。
---

## 摘要
我们提出Q引导值梯度匹配（Q-VGM），一种离策略强化学习方法，旨在解决微调流匹配视觉-语言-动作（VLA）策略中长期存在的挑战：如何高效地根据学习到的Q函数改进表达能力强的流匹配动作专家。有效的改进必须利用评论家的一阶（梯度）信息，但这对于流策略而言是困难的，因为在VLA规模下直接通过其多步去噪过程反向传播值函数在数值上不稳定，而策略梯度方法所需的可处理动作似然在迭代去噪条件下不可用。现有的基于值的方法要么通过完整的去噪链反向传播，要么仅在测试时使用评论家而不更新策略，或者将评论家改进的动作作为终端标签进行蒸馏而不监督速度场。Q-VGM通过利用VGG-Flow回避了这些问题，VGG-Flow是生成建模中流对齐的值梯度视角，它将值梯度转化为去噪时间上的值梯度场，而非不稳定的端到端目标。这不需要动作似然，也不需要经过去噪链反向传播，并且在固定的重放缓冲区上运行。评论家是一个基于紧凑RLT特征的逐层动作注入的动作敏感Cal-QL集成。Q-VGM实现了一种实用的少样本初始化后从经验中学习的范式：从少样本SFT pi0.5 VLA开始，该方法利用自生成的rollout数据显著提升任务性能，无需额外的专家监督。在LIBERO上，Q-VGM将平均成功率从75.0%提升至92.5%；在RoboTwin 2.0上，从76.4%提升至87.2%；在两个真实机器人桌面任务上，从40.0%提升至67.5%，在所有三种设置中均优于所有相同骨干、相同评论家的基线。

## Abstract
We propose Q-Guided Value-Gradient Matching (Q-VGM), an off-policy reinforcement learning (RL) method that tackles a long-standing challenge in fine-tuning flow-matching vision-language-action (VLA) policies: efficiently improving an expressive flow-matching action expert with respect to a learned Q-function. Effective improvement must exploit the first-order (gradient) information of the critic, but this is difficult for flow policies, because directly back-propagating the value through their multi-step denoising process is numerically unstable at VLA scale, while the tractable action likelihoods required by policy-gradient methods are unavailable under iterative denoising. Existing value-based methods either backpropagate through the full denoising chain, use the critic only at test time without updating the policy, or distill critic-improved actions as terminal labels without supervising the velocity field. Q-VGM sidesteps these issues by leveraging VGG-Flow, a value-gradient view of flow alignment in generative modeling that transforms value gradient into a denoising-time value-gradient field rather than an unstable end-to-end objective. This requires no action likelihoods and no backpropagation through the denoising chain, and operates on a fixed replay buffer. The critic is an action-sensitive Cal-QL ensemble over compact RLT features with per-layer action injection. Q-VGM enables a practical few-shot initialization then learn-from-experience paradigm: starting from a few-shot-SFT pi0.5 VLA, the method leverages self-generated rollout data to substantially improve task performance without additional expert supervision. On LIBERO, Q-VGM raises the average success rate from 75.0% to 92.5%; on RoboTwin 2.0, from 76.4% to 87.2%; and on two real-robot tabletop tasks, from 40.0% to 67.5%, outperforming all same-backbone, same-critic baselines across all three settings.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：流匹配（Flow-Matching）视觉-语言-动作（VLA）模型在机器人控制中表现出色，能通过迭代去噪生成连续动作。但在强化学习（RL）微调中，利用Q函数的梯度信息来改进策略面临两个根本困难：
  - 直接通过去噪链反向传播Q值在VLA规模下数值不稳定；
  - 流策略不提供可处理的动作似然，因此策略梯度方法无法直接使用。
- **现有方法的不足**：
  - 全链反向传播（如Diffusion-QL）不稳定；
  - 测试时Q选择/引导不更新策略；
  - Q蒸馏仅监督终端动作标签，忽略中间速度场。
- **整体含义**：Q-VGM提出了一种新的离策略RL微调框架，将值梯度转化为去噪时间上的速度场校正，从而稳定且高效地改进流匹配VLA策略，实现“少样本SFT初始化→自生成数据学习提升”的实用范式。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：借鉴VGG-Flow的最优控制视角，将策略改进建模为KL正则化随机最优控制问题，最优速度修正与去噪时刻的值梯度成正比。用动作Q函数代替终端奖励，构造速度场监督目标。
- **关键技术细节**：
  1. **动作敏感的分块评论家**：
     - 使用冻结的VLM骨干和RLT编码器提取状态特征（2048维RLT token + 本体感受）。
     - 采用Cal-QL集成（多个Q头），每个头在每层隐藏层重复注入归一化的动作块，保持对动作变异的敏感性。
     - 评论家完全离策略训练（使用数据集中的下一个动作块，而非当前策略采样）。
  2. **Q引导值梯度匹配**：
     - **前向估计**：对去噪过程中的每个中间状态，通过一步欧拉前向投影（使用冻结的基础速度）得到基线干净动作估计$\hat{A}_{k,\text{base}}$。
     - **迭代Q梯度上升**：对$\hat{A}_{k,\text{base}}$进行多步裁剪的Q梯度上升，得到候选动作$\hat{A}_{k,j}$。
     - **Keep-Best选择**：选择Q值最高的候选动作（包含j=0的原始动作，作为安全回退）。
     - **速度校正计算**：$h_k^{\text{eff}} = (\hat{A}_{k,\text{base}} - \hat{A}_{k,j^*}) / t_k$，将动作改进转化为去噪时间上的速度修正。
     - **门控调度**：使用$s(t_k) = (1 - t_k)^p$对校正强度加权，在靠近干净动作的时刻给予更大权重。
     - **残差速度匹配损失**：$L_{\text{align}} = \sum_k s(t_k) \| (v_\theta(x_k,t_k,s) - v_{\text{base}}(x_k,t_k,s)) - \text{sg}[h_k^{\text{eff}}] \|_2^2$，梯度只流向当前策略速度预测，其余均停止梯度。
  3. **训练流程**：在固定rollout缓冲区上训练评论家→冻结评论家→用Q-VGM损失微调策略动作专家（VLM骨干冻结）。
- **算法流程文字说明**：
  - 采样一个状态、初始噪声；
  - 对去噪时间步k循环：
    - 预测当前策略速度；
    - 通过基础速度前向估计得到基线动作；
    - 迭代Q梯度上升获得候选动作，选择Q最高的；
    - 计算速度校正；
    - 添加匹配损失；
    - 用当前策略速度一步前进（停止梯度）；
  - 更新策略参数。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集/场景**：
  - **LIBERO**：4个套件（Spatial, Object, Goal, Long），共40个桌面操作任务，7-DoF末端执行器动作空间。
  - **RoboTwin 2.0**：10个双臂操作任务，14-DoF qpos动作空间，涉及抓取、放置、协调、工具使用。
  - **真实机器人桌面任务**：Pick Peach（捡桃子）和 Stack Bowls（叠碗），7-DoF机械臂+2个RGB-D相机。
- **Benchmark**：各任务的成功率的平均值。
- **对比方法**（所有基线使用相同SFT初始策略、rollout数据、RLT特征和Cal-QL评论家）：
  - 测试时Q选择（Test-time Q Selection）
  - 测试时Q引导（Test-time Q Guidance，即推理时用梯度微调动作）
  - Q改进动作蒸馏（Q-Improved Action Distillation，将评论家改进的动作作为监督标签）
  - Diffusion-QL（全链反向传播）
  - 初始SFT策略作为参考下限。

### 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量和训练时长。仅提及VLM骨干冻结、策略动作专家更新约3.9M参数/头（集成评论家）。文中未提供详细的算力消耗信息。

### 5. 实验数量与充分性

- **实验数量**：
  - LIBERO：4个套件×每个套件50次rollout（共200次评估），以及多组实验（初始策略、4种基线、Q-VGM）。
  - RoboTwin 2.0：10个任务×50次rollout，5种方法对比。
  - 真实机器人：2个任务×20次物理试验，两种方法对比。
  - 消融实验：LIBERO上8个变体（评论家侧：RLT vs ResNet、无逐层动作注入、单头；策略侧：无Keep-Best、全步门控、无冻结基础锚）。
- **充分性**：
  - 较为充分：覆盖多个仿真和多任务、真实场景；消融实验系统验证了各组件贡献；基线方法均使用相同骨干和评论家，公平性较好。
  - 但可改进点：LIBERO和RoboTwin的任务总数和多样性有限，真实机器人仅2个任务、20次试验，统计显著性可能不足；未提供方差或置信区间。Diffusion-QL基线在VLA规模下效果差，但论文指出其不稳定性与任务相关，未深入分析。

### 6. 论文的主要结论与发现

- Q-VGM在仿真和真实场景中均一致且显著优于所有相同骨干、相同评论家的基线：
  - LIBERO：平均成功率从75.0%提升至92.5%（+17.5%），尤其Long任务从56.0%→84.0%。
  - RoboTwin 2.0：从76.4%提升至87.2%（+10.8%），在低SFT任务上增益更大（如place shoe +16pp）。
  - 真实机器人：平均成功率从40.0%提升至67.5%（+27.5%）。
- 消融实验表明：RLT特征、逐层动作注入、集成评论家、Keep-Best选择、门控调度、冻结基础锚均为关键组件。
- 验证了“少样本SFT初始化→自生成数据学习提升”范式的有效性，无需额外专家监督。

### 7. 优点

- **方法论创新**：将最优控制视角与VGG-Flow结合，将值梯度转化为速度场修正，避免了全链反向传播的不稳定性，也无需计算动作似然，适合流匹配VLA框架。
- **实用性强**：完全离策略，仅需固定rollout缓冲区，训练稳定；推理时无需额外评论家或搜索，策略直接生成动作。
- **动作敏感评论家设计**：每层注入动作、使用RLT特征和Cal-QL集成，有效提高了梯度品质。
- **广泛的验证**：涵盖仿真大套件、双臂复杂任务和真实机器人，综合性能提升显著。
- **消融实验系统**：清晰展示了各设计选择的贡献。

### 8. 不足与局限

- **对评论家质量的依赖**：Q梯度在动作分布外可能不可靠，虽然用梯度裁剪和Keep-Best缓解，但未完全解决分布外泛化问题。
- **缩放性与泛化性**：任务数量增加时，Q学习的难度上升（论文承认这一局限）。未讨论多任务评论家共享时的收敛性。
- **基准覆盖有限**：未在更多机器人平台（如移动操作、多指灵巧手）或更开放环境（如MineDojo、Habitat）上评估。
- **统计信息缺失**：仅报告平均成功率，未提供方差、置信区间或显著性检验，难以判断改进的稳定性。
- **算力细节缺失**：未说明资源和训练时间，可复现性受影响。
- **基数限制**：真实机器人仅20次试验，可能不足以排除偶然性。此外，仅对比了同类评论家基线，未与在线RL或PPO类方法比较（但后者需似然，难以适用）。
- **仅评估离策略微调**：未探索在线交互或迭代收集数据后的性能天花板。

（完）
