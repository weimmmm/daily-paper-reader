---
title: "AgenticNav: Zero-Shot Vision-and-Language Navigation as a Tool-Calling Harness"
title_zh: AgenticNav：作为工具调用框架的零样本视觉与语言导航
authors: "Yijian Li, Changze Li, Hantian Shi, Jiaying Luo, Jiyuan Cai, Ming Yang, Tong Qin"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10577v1"
tags: ["query:embodied-rl"]
score: 9.0
evidence: 零样本连续环境视觉语言导航，将VLM作为工具调用接口，显式利用动作和深度信息
tldr: 零样本连续环境视觉语言导航（VLN-CE）近期借助大型视觉语言模型（VLM）可行，但现有方法依赖学习航点预测器，限制动作空间且未能有效利用深度输入。记忆管理常累积冗余历史或检索跨场景经验，削弱零样本能力。本文提出AgenticNav，将动作、深度和记忆暴露为可调用工具：动作工具允许VLM直接选择RGB目标像素转换为运动；深度工具按需查询像素深度；记忆工具提供紧凑地图和召回工具选择性回顾历史。在R2R-CE基准上达到零样本新SOTA，真实世界验证泛化性。消融表明动作工具设计优于传统航点预测，深度工具和智能记忆进一步贡献性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10577v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1453, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1363, \"height\": 808, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1445, \"height\": 279, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10577v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1440, \"height\": 291, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10577v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 912, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10577v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10577v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1350, \"height\": 346, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10577v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 304, \"label\": \"Table\"}]"
motivation: 现有零样本VLN-CE方法依赖学习航点预测器，限制动作空间且未有效利用深度输入；记忆管理冗余或依赖跨场景经验，削弱零样本设置。
method: AgenticNav将动作、深度和记忆作为可调用工具暴露给VLM：动作工具允许VLM直接选择RGB像素以转换为运动；深度工具按需提供像素深度；记忆工具提供紧凑地图和召回工具选择性回顾历史观察。
result: 在R2R-CE基准上，使用相同VLM骨干达到零样本新SOTA；真实世界验证显示零样本泛化性优于先前方法。
conclusion: 动作工具设计优于传统航点预测器，深度工具和智能记忆进一步贡献导航性能。
---

## 摘要
在连续环境中的零样本视觉与语言导航（VLN-CE）最近随着大型视觉语言模型（VLM）的出现而变得可行。然而，现有方法通常依赖学习到的航点预测器来提出可导航的动作，这严重限制了模型的动作空间，并且未能有效利用深度输入。此外，记忆通常通过累积包含大量无关上下文的冗长文本或视觉历史，或者通过检索跨场景经验来处理，这削弱了零样本设置。在本文中，我们将零样本VLN-CE重新构想为VLM与环境之间的代理接口，并提出了AgenticNav，一个轻量级导航框架，将动作、深度和记忆作为可调用工具暴露出来。动作工具允许VLM直接从RGB观测中选择目标像素，并将其转换为可执行运动，而不是从预测的航点中选择。深度通过按需像素深度工具暴露，使VLM仅在需要时请求精确的度量距离。对于记忆，AgenticNav提供一张紧凑的地图图像，总结历史轨迹，并配有一个召回工具，允许VLM选择性地重新访问过去的视觉观测，而不会使提示上下文过载。在R2R-CE基准测试中，给定相同的VLM骨干网络，AgenticNav在零样本方法中建立了新的最先进性能。真实世界验证进一步突出了其相比先前方法的零样本泛化能力。消融实验表明，我们的动作工具设计优于传统航点预测器，并且深度工具和代理记忆进一步促进了导航性能。

## Abstract
Zero-shot vision-and-language navigation in continuous environments (VLN-CE) has recently become feasible with large vision-language models (VLMs). However, existing methods typically rely on learned waypoint predictors to propose navigable actions. This severely limits the model's action space and fails to leverage depth inputs effectively. Moreover, memory is commonly handled by accumulating long textual or visual histories with substantial irrelevant context, or by retrieving cross-episode experiences, which weakens the zero-shot setting. In this paper, we rethink zero-shot VLN-CE as an agentic interface between the VLM and the environment, and present AgenticNav, a lightweight navigation harness that exposes action, depth, and memory as callable tools. Instead of choosing from predicted waypoints, the action tool allows the VLM to directly select a target pixel in RGB observations, converting it into executable motion. Depth is exposed through an on-demand pixel-depth tool, enabling the VLM to request precise metric distances only where they matter. For memory, AgenticNav provides a compact map image summarizing the historical trajectory, paired with a recall tool that allows the VLM to selectively revisit past visual observations without overwhelming the prompt context. On the R2R-CE benchmark, AgenticNav establishes new state-of-the-art (SOTA) performance among zero-shot methods given the same VLM backbone. Real-world validation further highlights its zero-shot generalization compared to prior methods. Ablations show that our action tool design outperforms traditional waypoint predictors, and that depth tool and agentic memory further contribute to navigation performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在零样本连续环境视觉语言导航（VLN-CE）中，现有方法严重依赖**训练好的航点预测器**，该预测器仅输出有限个候选动作，限制了VLM的动作空间；同时深度信息被“隐藏”在预测器中，VLM无法有效利用；记忆管理要么累积冗长的历史上下文（含大量无关信息），要么检索跨场景经验（削弱零样本假设）。
- **整体含义**：作者认为零样本VLN-CE的性能瓶颈**不仅是VLM本身的推理能力，更是VLM与环境之间的接口设计**。因此，他们提出将导航重构成**工具调用框架（Tool-Calling Harness）**，让VLM通过调用原子化工具来感知（深度）、记忆（召回）和行动（移动/停止），从而释放VLM的视觉推理潜力，实现更好的零样本泛化。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将零样本VLN-CE视为VLM与环境的**代理接口**，将动作、深度和记忆封装为**可调用的工具**，VLM按需调用这些工具来获取信息和执行动作，无需依赖任何训练的导航模块。

### 关键技术细节
1. **工具集**（共4个工具）：
   - **行动工具 `move_to(k, u, v)`**：允许VLM直接在**RGB图像**上选择目标像素（指定视角k和归一化坐标u,v）。系统通过深度反投影、障碍物安全走廊检查（检查该方向是否在机器人半径内存在障碍物），将像素转换为**可执行的运动命令（转向角θ、前进距离ρ）**；若不安全则返回 `Reselect`。
   - **深度工具 `query_depth(P)`**：VLM可批量查询一组像素的**精确度量深度**，返回包括深度值、5×5邻域统计（min/mean/median）以及若该点被选为目标的动作预览（相对偏航角、可执行距离）。深度查询**不移动机器人**，仅提供数值证据。
   - **记忆工具 `recall(p, k)`**：VLM在步骤中可回忆**过去决策点**（p）的某一视角图像（k），从缓存中取出并追加到当前提示中，避免提示过长。
   - **停止工具 `stop()`**：终止回合。

2. **记忆管理**：
   - 维护一个**紧凑的地图图像** `Bt`（轨迹鸟瞰图），默认包含在提示中。
   - 另外维护一个**缓存 `Ct`**，存储所有历史观测图像，但**默认不插入提示**，仅在VLM显式调用`recall`时才加载，实现选择性记忆。

3. **动作执行流程**：
   - VLM首先可调用 `query_depth` 获取空间证据，比较候选点的深度。
   - 然后调用 `move_to` 指定目标像素，系统进行安全校验；若通过则执行连续动作（θ, ρ）；若被拒绝（`Reselect`），VLM需重新选择。
   - 整个流程中，**数值几何运算（深度反投影、障碍物检查）由确定性工具完成**，VLM只负责推理和像素选择。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

### 数据集与Benchmark
- **模拟环境**：Habitat模拟器，使用**R2R-CE**数据集的**val unseen 100个episodes**（与近期的零样本VLN-CE工作使用完全相同的协议）。
- **真实世界**：部署在四轮全向机器人（Intel RealSense D435 RGB-D相机、LiDAR、FAST-LIO定位）上，定义**30个真实世界导航任务**，涵盖实验室、办公室、室外庭院三种场景，强调标志识别、远距离导航、模糊指令、精确到达。

### 对比方法
- **模拟对比**（零样本方法）：
  - SmartWay-GPT-5.5（作者复现）
  - Open-Nav（GPT-4、Gemini-2.5-pro、Gemini-3-Flash）
  - EvoNav-Gemini-2.5-pro（跨场景经验检索，不视为严格零样本）
  - MapGPT-CE、DiscussNav、NavGPT-CE、Random、LXMERT等
  - 同时列出有监督方法CMA、RecBERT、BEVBert、ETPNav作为参考。
- **真实世界对比**：SmartWay-GPT-5.5（12视图），AgenticNav采用1视图或4视图。
- **消融实验**：替换行动工具为航点预测器、移除深度工具、移除记忆、仅保留地图等。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量及训练时长。因为AgenticNav本身**不训练任何模块**，仅使用冻结的VLM（GPT-5.5、Gemini-2.5-Pro等），所以算力主要消耗在VLM推理上。但论文未给出推理时的具体硬件配置或能耗数据。

## 5. 实验数量与充分性

- **模拟实验**：一个主要表格（Table 1）对比了10+种方法，包含作者复现的基线（标注*）。
- **消融实验**：
  - 工具组件消融（Table 3）：7个变体，分别移除行动工具、深度工具、记忆、地图等。
  - VLM骨干消融（Table 4）：5种不同VLM（GPT-5.5、Gemini-2.5-Pro、Gemini-3-Flash、MiMo-V2.5、Qwen3.6-27B）。
- **真实世界实验**：30个episodes，统计成功率与平均导航误差，并给出了定性分析（3个示例）。
- **公平性**：模拟对比中，所有复现基线使用与AgenticNav相同的VLM核心和实验设置；真实世界使用相同机器人、路线和指令。**实验总体充分**，覆盖不同组件和不同骨干。
- **不足**：真实世界仅30个episodes，样本量较小；未在更大型的真实场景（如多楼层）评估；未进行用户研究或跨机器人平台验证。

## 6. 论文的主要结论与发现

1. **AgenticNav在R2R-CE上实现零样本SOTA**：GPT-5.5骨干下SR=55%, SPL=48.41%，比相同骨干的SmartWay高出11个百分点SR和13.37个百分点SPL。
2. **工具设计优于航点预测**：将行动工具替换为航点预测器导致SR下降5个百分点（55→50），SPL下降4.42个百分点。
3. **按需深度工具有效**：移除深度工具SR降至42%；即使强制输入深度图像也仅恢复至53%，低于55%。
4. **选择性记忆优于全历史**：移除全部记忆SR降至41%，仅保留地图图像达到51%，完整记忆（地图+召回）达55%，证明召回工具具有附加值。
5. **真实世界零样本泛化性**：AgenticNav在30个真实任务中达到46.7% SR（4视图），翻倍于SmartWay的23.3%，且在室外场景优势最大。
6. **VLM决策质量是瓶颈**：失败分析中，88.9%的模拟失败和71.4%的真实失败源于VLM推理错误（如选错分支、房间或实例），而非工具或控制失效。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：巧妙地将VLN-CE重构为**工具调用范式**，让VLM直接操作像素、按需查询深度和选择性回忆，摆脱了传统航点预测器的限制。
- **零样本纯粹性**：不依赖任何跨场景经验或训练模块（除冻结VLM），严格遵守零样本假设。
- **深度利用更充分**：按需像素级深度查询避免了VLM直接处理深度图的困难，提供了精确的度量证据。
- **记忆高效且可控**：紧凑地图+选择性召回避免了上下文窗口膨胀，同时保留了关键视觉细节。
- **真实世界验证**：不仅在模拟中取得SOTA，还在真实机器人上展示了明显的sim-to-real泛化优势，增强了方法的说服力。
- **消融实验全面**：分别隔离每个工具组件和不同VLM骨干，清晰展示了各部分贡献。

## 8. 不足与局限

- **VLM决策错误主导失败**：方法仍受限于VLM的推理质量和幻觉现象，这是当前所有LLM/VLM方法的共性挑战。
- **API依赖与实时性**：使用远程VLM API引入延迟和网络不稳定，不利于实时部署；论文未对比本地部署的延迟。
- **硬件假设**：假设准确的自定位、可靠的深度传感器和稳定的通信（API），在实际部署中可能不满足。
- **真实世界评估规模小**：仅30个任务，每个场景约10个，统计显著性可能不足；未包含动态障碍物、光照变化等更复杂的真实条件。
- **未报告算力成本**：缺乏VLM推理时的硬件资源和能耗数据，难以评估实际运行开销。
- **仅评估单一模拟协议**：使用100个episodes，虽然与近期工作一致，但R2R-CE还有更大的test unseen划分，未评估。
- **缺少与更多基线（如EvoNav）的直接公平比较**：EvoNav因非零样本（检索跨场景经验）而未纳入主要表格，但论文仍与其对比SR（AgenticNav 49 vs EvoNav 43），但需注意EvoNav设置不同。

（完）
