---
title: Neuro-Symbolic Injection of LTLf Constraints in Autoregressive Reinforcement Learning Policies
title_zh: 自回归强化学习策略中LTLf约束的神经符号注入
authors: "Ashkan Ansarifard, Matteo Mancanelli, Elena Umili, Fabio Patrizi"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08312v1"
tags: ["query:embodied-rl"]
score: 9.0
evidence: 强化学习与时态逻辑约束
tldr: 离线强化学习面临时间扩展约束下的序列决策问题，现有Transformer方法仅优化奖励而忽略高层时间需求。本文提出神经符号框架，将LTLf约束编译为DFA，通过可微满意度信号和逻辑损失正则化注入Transformer策略。实验表明，该方法在导航任务中显著提升约束满足率，同时保持与基线相当的回报。该工作证明了神经符号结合在离线RL中的有效性，为可解释、合规策略学习提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08312v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 1000, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1632, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1650, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1497, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1496, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1491, \"height\": 922, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08312v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1323, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08312v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1368, \"height\": 307, \"label\": \"Table\"}]"
motivation: 现有Transformer强化学习方法仅优化奖励，无法处理LTLf表达的时间扩展约束，导致智能体行为可能违反高层规范。
method: 将LTLf公式编译为DFA，利用其状态进展生成可微满意度信号，作为正则化项融入Transformer策略的离线训练。
result: 在导航环境中，该方法相比基线在满足安全性和可达性约束上提升显著，且回报未出现明显下降。
conclusion: 通过神经符号注入LTLf约束，可有效规范Transformer策略行为，同时维持奖励性能，为离线强化学习引入背景知识提供可行方案。
---

## 摘要
在这项工作中，我们研究了在有限轨迹线性时序逻辑（LTLf）表达的时延任务约束下的离线强化学习（RL）。最近，诸如Trajectory Transformers和Decision Transformers等基于Transformer的方法已被采用，将RL视为一个序列建模问题。然而，这些方法纯粹针对奖励进行优化，并未考虑高阶时间需求。在这里，我们引入了一个神经符号框架，将LTLf背景知识注入到这类基于Transformer的RL策略中。我们的方法将LTLf公式编译成确定性有限自动机（DFA），并通过可微表示和基于逻辑的损失函数将其整合到学习过程中。特别地，我们从DFA进展中推导出可微的满足信号，并将其作为训练过程中的正则化项。由此产生的方法在不同模型之间是架构无关的。我们在带有涵盖安全性和可达性时间属性组合的规范集的导航环境中评估了所提出的框架。实验结果表明，融入背景知识不仅改善了约束满足，而且与原始基线相比保持了竞争力的回报。

## Abstract
In this work we study offline reinforcement learning (RL) under temporally extended task constraints expressed in Linear Temporal Logic over finite traces (LTLf). Recently, transformer-based approaches such as Trajectory Transformers and Decision Transformers have been adopted to address RL as a sequence modeling problem. However, these methods optimize purely for reward and do not account for high-level temporal requirements. Here, we introduce a neurosymbolic framework that injects LTLf background knowledge into such transformer-based RL policies. Our approach compiles LTLf formulas into deterministic finite automata (DFAs) and integrates them into the learning process through a differentiable representation and a logic-based loss function. In particular, we derive differentiable satisfaction signals from DFA progression and use them as a regularization term during training. The resulting method is architecture-agnostic across different models. We evaluate the proposed framework on navigation environments with specification suites covering combinations of safety and reachability temporal properties. Experimental results show that incorporating background knowledge not only improves constraint satisfaction, but also maintains competitive return compared to vanilla baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：离线强化学习中，基于Transformer的策略（如Trajectory Transformer、Decision Transformer）将RL视为序列建模，仅通过最大似然优化轨迹预测损失，忽略了高层时序约束（如“始终避开危险”或“最终到达目标”）。这些约束通常由线性时序逻辑（LTLf）表达，涉及整个轨迹的时序依赖，传统方法无法保证满足。
- **重要性**：在安全关键领域（机器人、自动驾驶等），策略必须遵守明确的时间规范；离线设置下无在线交互修正，违反约束可能带来不可逆后果。
- **挑战**：现有方法要么依赖手工设计的奖励塑形，要么局限于安全片段（safety fragment），或需要环境模型（如shielding）。
- **本文贡献**：提出神经符号框架，将LTLf约束编译为确定性有限自动机（DFA），通过可微逻辑损失正则化Transformer策略训练，实现约束满足与回报的平衡，且架构无关。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 将LTLf公式 $\phi$ 编译为等价DFA $A_\phi$。
  - 在训练时，自回归模型输出下一token的概率分布，通过Gumbel-Softmax采样得到可微的软轨迹样本。
  - 利用DeepDFA（神经概率松弛DFA）计算每条软轨迹满足$\phi$的概率 $\hat{P}_\theta^{\models\phi}$。
  - 定义逻辑损失 $L_\phi = -\log(\hat{P}_\theta^{\models\phi})$，并与监督损失 $L_\mathcal{D}$（交叉熵）组合为总损失 $L = \alpha L_\phi + (1-\alpha)L_\mathcal{D}$，其中$\alpha$为权衡参数。

- **关键技术细节**：
  - **LTLf编译为DFA**：使用ltlf2DFA工具自动转换，并扩展处理特殊的EOT（end-of-trace）令牌，确保完整轨迹才被接受。
  - **可微采样**：采用Gumbel-Softmax放松离散采样，使梯度可回传。
  - **DeepDFA**：将DFA的转移矩阵表示为可微参数化层，输入软符号，输出状态分布和最终接受概率。
  - **逻辑适配器**：统一处理数据集构建、token到原子命题映射、DFA展开和逻辑损失评估，确保语义一致性。
  - **流程**：训练时，模型自回归生成完整轨迹（含EOT），通过DeepDFA计算满足概率，优化复合损失；推理时使用贪婪解码。

## 3. 实验设计

- **环境**：ColourBomb（网格导航），包含起点(S)、三种颜色目标(P/Y/U，终止且正奖励)、炸弹(B，终止且负奖励)、墙壁(W)。每步有惩罚，有限回合。
- **规范**：
  - Safety: $G(\neg bomb)$（始终避开炸弹）
  - Reach-while-Safe: $G(\neg bomb) \land F(goal)$（始终避开炸弹且最终到达目标）
- **对比方法**：
  - 基线：vanilla Trajectory Transformer (TT) 和 Decision Transformer (DT)，即 $\alpha=0$。
  - 逻辑正则化版本：$\alpha \in \{0.01,0.05,0.1,0.2,0.4,0.6,0.8\}$。
  - 推理统一使用贪婪解码。
- **评估指标**：Return、Satisfaction Rate（约束满足率）、Goal Rate（到达目标率）、Bomb Hit Rate（命中炸弹率）。

**注意**：论文未与其他Safe RL方法（如约束MDP、shielding、成本条件化DT）进行显式对比，仅在同一框架下比较不同$\alpha$的影响。

## 4. 资源与算力

论文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。仅提供了开源代码仓库链接（GitHub），但未在正文或附录中详述硬件配置。

## 5. 实验数量与充分性

- **数量**：
  - 对DT和TT分别进行了两个规范下的$\alpha$扫描（7个非零值 + 1个基线），每个配置重复3次并报告均值和标准差。
  - 同时报告了决策Transformer的定量结果表（表1）和轨迹Transformer的定量结果表（表2）。
  - 约 $2 \text{ (架构) } \times 2 \text{ (规范) } \times (7+1) \text{ (alpha) } \times 3 \text{ (random seeds) } = 96$ 个实验点，但实际论文只展示了部分典型值。
- **充分性评价**：
  - **优点**：覆盖了不同正则化强度，进行了多随机种子统计，证明了方法的有效性。
  - **不足**：仅在一个环境（ColourBomb）上测试，缺乏泛化性验证；未与其他现有Safe RL方法（如Cost-conditioned DT、Shielding等）进行直接公平比较；未消融DeepDFA与硬DFA评估的差异、采样策略影响等。实验设计基本客观，但覆盖范围有限。

## 6. 论文的主要结论与发现

- **决策变压器（DT）**：
  - 低/中正则化（$\alpha \leq 0.4$）无改善；$\alpha=0.6$实现零炸弹但无法到达目标；$\alpha=0.8$同时达到100%安全满足、100%目标到达、零炸弹，回报显著提升（从-1.46到-0.78）。
- **轨迹变压器（TT）**：
  - Safety规范下，$\alpha=0.8$最佳，满足率从0.353提升至0.499，回报从-0.929提升至-0.792。
  - Reach-while-Safe规范下，$\alpha=0.1$最佳，满足率从0.018提升至0.047，回报从-0.929提升至-0.903；但整体满足率仍较低（<5%），说明该约束更难。
- **整体**：逻辑正则化能有效引导策略行为，减少违反约束的行为，同时维持或略优回报。$\alpha$参数敏感，需要在满足率和任务性能间权衡。
- **DT上的提升比TT更显著**，但原因未深入分析（论文提及留待未来研究）。

## 7. 优点

- **创新性**：首次将LTLf约束以可微正则化方式集成到离线序列RL（TT/DT）中，无需修改数据或奖励函数。
- **架构无关性**：方法仅依赖自回归模型输出概率，可适用于任意类似生成模型。
- **表达能力**：支持任意LTLf公式（包括非安全片段，如liveness），优于仅处理安全属性的shielding等方法。
- **一致性设计**：通过逻辑适配器统一了数据集序列化、token映射、DFA评估，避免了语义不匹配。
- **实验可视化**：清晰展示了$\alpha$扫描下各项指标的折线图，便于观察趋势。

## 8. 不足与局限

- **实验规模有限**：仅在单一导航环境（ColourBomb）上评估，未在复杂机器人控制、多智能体或高维状态空间等场景验证，泛化性存疑。
- **缺乏与现有工作对比**：未与约束MDP方法、成本条件化DT（如Saformer、Constrained DT）、shielding等方法进行系统性比较，难以判断相对优势。
- **TT上的改善幅度较小**：满足率从0.353提升到0.499（safety），reach-while-safe仅从0.018到0.047，实际绝对改进有限；DT虽提升大但可能因基线太差。未分析原因。
- **超参数敏感**：$\alpha$需手动调优，不同环境和规范最佳值差异大，实际应用可能需要额外调参成本。
- **仅训练时正则化**：未结合测试时约束解码（如受约束束搜索），论文提及留作未来工作，但当前方法在推理时无法保证约束满足（贪婪解码可能产生未受约束轨迹）。
- **可扩展性未讨论**：DeepDFA和DFA编译在长轨迹、大状态空间下的计算开销未分析。
- **未消融组件**：未单独分析Gumbel-Softmax、DeepDFA、蒙特卡洛样本数等组件影响。

（完）
