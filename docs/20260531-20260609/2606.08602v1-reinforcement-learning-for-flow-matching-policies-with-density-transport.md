---
title: Reinforcement Learning for Flow-Matching Policies with Density Transport
title_zh: 带密度传输的流匹配策略的强化学习
authors: "Boshu Lei, Kostas Daniilidis, Antonio Loquercio"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08602v1"
tags: ["query:embodied-rl"]
score: 9.0
evidence: 流匹配策略的在线强化学习
tldr: 针对现有强化学习微调流匹配策略存在有偏梯度或牺牲多模态能力的问题，提出RLDT算法。该算法将策略改进视为动作密度传输，利用Stein变分梯度下降构建传输场，并通过期望目标估计稳定训练。在连续控制任务中，RLDT在奖励质量和收敛速度上优于基线，支持密集与稀疏奖励、状态与视觉任务。为流匹配策略提供无偏且高效的在线微调方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08602v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1382, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1434, \"height\": 720, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1423, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1439, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1441, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08602v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1438, \"height\": 482, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08602v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08602v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 186, \"label\": \"Table\"}]"
motivation: 现有RL方法微调流匹配策略时存在有偏梯度或损失多模态建模能力的问题。
method: 从最大熵RL目标用SVGD构建传输场，通过期望目标估计近似中间动作，避免不稳定时间反向传播。
result: RLDT在连续控制任务上奖励质量和收敛速度优于基线，涵盖密集/稀疏奖励和视觉长程任务。
conclusion: RLDT为流匹配策略提供了无偏梯度且保留多模态的在线RL微调框架。
---

## 摘要
我们提出了一种在线强化学习（RL）算法，用于微调连续控制问题中的流匹配策略。我们的关键见解是将基于RL的策略改进视为将动作密度向高奖励区域传输，这自然与流匹配模型的传输公式一致。先前的方法要么近似当前或最优策略分布，要么依赖于蒸馏，这会引入有偏梯度或牺牲多模态建模能力。相比之下，我们提出的基于密度传输的强化学习方法（称为RLDT）利用Stein变分梯度下降（SVGD）从最大熵RL目标构建一个传输场。然后，它微调预训练的流匹配策略以与该场对齐。使用该对齐目标进行训练并非易事，因为流匹配策略通过多步过程生成动作，使得直接的基于梯度的优化具有挑战性。为了克服这一挑战并稳定训练，我们通过期望目标估计从中间去噪步骤近似策略动作。这使得传输场更新能够传播到网络参数中，而无需通过时间进行不稳定的反向传播。实验结果表明，RLDT在奖励质量和收敛速度方面优于竞争基线。该性能在多种连续控制任务中保持一致，包括密集和稀疏奖励，以及基于状态和视觉的长期机器人操作。项目网页是https://rpfey.github.io/rldt/。

## Abstract
We present an online reinforcement learning (RL) algorithm for fine-tuning flow-matching policies in continuous-control problems. Our key insight is to view RL-based policy improvement as a transport of action densities towards regions of high reward, which naturally aligns with the transport formulation of flow matching models. Prior methods either approximate the current or optimal policy distribution or resort to distillation, which introduces biased gradients or sacrifices multimodal modeling capacity. In contrast, our approach for RL with Density Transport, which we name \emph{RLDT}, constructs a transport field from a maximum-entropy RL objective using Stein Variational Gradient Descent (SVGD). Then, it finetunes a pretrained flow matching policy to align with this field. Training with this alignment objective is nontrivial because flow-matching policies generate actions via a multi-step process, making direct gradient-based optimization challenging. To overcome this challenge and stabilize training, we approximate policy actions from intermediate denoising steps via expected-target estimation. This allows the transport-field update to propagate into the network parameters without unstable backpropagation through time. Experimental results demonstrate that RLDT outperforms competitive baselines in reward quality and convergence speed. This performance holds across diverse continuous-control tasks, encompassing both dense and sparse rewards, as well as state- and vision-based long-horizon robot manipulation. The project webpage is \href{https://rpfey.github.io/rldt/}{https://rpfey.github.io/rldt/}.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：视觉-语言-动作模型（VLA）在大规模预训练后可作为机器人基础策略，但预训练策略可能在特定下游任务上不是最优的（例如训练数据次优、分布外失败）。强化学习（RL）提供了一种在线微调预训练流匹配策略的原则性方法。
- **核心挑战**：流匹配策略通过多步去噪过程生成动作，这使得：(1) 无法直接获取任务特定的最优策略样本；(2) 计算策略的对数似然非常昂贵且不可行。
- **现有局限**：先前的RL方法要么使用对数似然的近似下界（引入有偏梯度），要么通过蒸馏将多步策略压缩为少步模型（牺牲多模态表达能力），要么需要反向传播通过整个去噪链条（不稳定）。

### 2. 论文提出的方法论

- **核心思想**：将基于RL的策略改进重新解释为动作概率密度向高奖励区域的传输（transport），即定义传输映射 T = id + εφ，其中 φ 是传输速度场。流匹配策略本身学习从简单先验到目标动作分布的时变传输场，因此天然适合对齐。
- **关键技术细节**：
  - 从最大熵RL目标推导出最优策略 π*(a|s) ∝ exp(Q(s,a)/κ)，并使用Stein变分梯度下降（SVGD）构建传输场 φ*(a)。SVGD包含一个吸引力项（∇ₐQ(s,a)）和一个排斥项（∇ₐ'k(a',a)），防止粒子坍缩，促进探索。
  - 为了解决多步去噪过程中的梯度稀释问题，利用期望目标预测（expected-target estimation）——从中间去噪步骤 a_{t,τ} 估计出对应的“干净”动作 a†_{t,τ} = a_{t,τ} + (1-τ) vθ(a_{t,τ}, τ)，该预测位于动作流形上，可以直接计算SVGD更新。
  - 推导出最终的RL损失 L_RL = E[ vθ(a_{t,τ}, τ)ᵀ φ*(sg(a†_{t,τ})) / (T(1-τ)) ]，该损失直接惩罚流场与传输场之间的不一致，无需计算对数似然，也无需通过时间反向传播。
  - 附加正则项：一致性损失 L_consist（保持流场线性）和约束损失 L_constraint（Fisher散度，使微调策略接近预训练策略）。
- **算法流程**（文字说明）：
  1. 用流匹配策略 πθ 与环境交互，收集经验存入回放缓冲区。
  2. 更新Q网络（双Q集成，使用标准TD目标）。
  3. 策略更新：采样 τ ~ U[0,1] 和初始噪声 a_{t,0} ~ N(0,I)，运行ODE得到中间 a_{t,τ}；计算预期目标 a†；计算SVGD方向 φ*(a†)；按损失 L_actor = L_RL + λ1 L_consist + λ2 L_constraint 更新 θ。
  4. 软更新目标网络。

### 3. 实验设计

- **数据集/场景**：
  - OpenAI Gym（密集奖励）：HalfCheetah, Hopper, Walker2d；状态输入，动作块大小 Ta=4。
  - Furniture-Bench（稀疏奖励、长程多阶段）：One-Leg（单阶段）和 Lamp（两阶段），低/中随机性；状态输入，Ta=8。
  - Robomimic（稀疏奖励、视觉输入）：Square（Ta=4）和 Transport（Ta=8）；RGB图像+本体感觉输入。
- **Benchmark**：使用相同的预训练流匹配策略（基于DPPO网络架构），仅改变训练目标。对比方法包括：
  - DPPO：用所有去噪潜变量的联合概率近似对数似然（开环估计）。
  - ReinFlow：类似DPPO但使用SDE/可学习噪声。
  - FPO++：用ELBO下界近似对数似然，并采用不对称截断。
  - QAM：用伴随匹配构造逐步骤目标。
- **对比设置**：每个实验运行3个不同随机种子，报告均值和标准差。

### 4. 资源与算力

- 文中明确说明（附录B.2）：
  - OpenAI Gym：单块NVIDIA 2080Ti GPU，训练约1.5 GPU小时。
  - Furniture-Bench：单块NVIDIA A40 GPU，训练约48 GPU小时。
  - Robomimic：单块NVIDIA A40 GPU，训练约30 GPU小时。
- 未提及整体训练集群规模，但可推断为单GPU实验。

### 5. 实验数量与充分性

- **主要实验数量**：三大类环境共7个子任务（Gym 3个、Furniture-Bench 2个配置×2种随机性=4个、Robomimic 2个），每个3种子，共至少21个实验轨迹。
- **消融实验**：
  - 温度系数 κ0（{0, 1e-4, 1e-3}）在Robomimic Square/Transport上。
  - 粒子数 K（4, 8, 16）在Gym任务上。
  - 核函数对比：RBF核 vs Delta核（无排斥项）在Gym和Robomimic/Furniture-Bench上。
  - 梯度范数分布分析（RLDT vs QAM）在Hopper上。
- **充分性评估**：实验覆盖了密集/稀疏奖励、状态/视觉、短/长程任务，消融研究关键超参数和组件。对比了四种代表性基线，且包含了与最相关方法QAM的细节比较。实验设计较为充分和公平。

### 6. 主要结论与发现

- RLDT在所有测试任务上均优于基线，在奖励质量和收敛速度上表现更好。
- 在密集奖励Gym任务中，RLDT显著领先；在稀疏奖励机器人任务中，RLDT在Visual Square和Lamp Med上尤其突出（Lamp Med成功率70% vs DPPO 30%）。
- 梯度范数分析表明，RLDT的梯度过ODE步长分布更均匀且稳定，而QAM早期步长梯度极小，导致训练不稳定。
- 核函数消融表明，Delta核（无排斥项）在密集奖励任务中尚可，但在稀疏奖励任务中导致性能下降甚至发散，说明SVGD的排斥项对保持探索和稳定性至关重要。
- RLDT对粒子数K不敏感（4个粒子即可），对温度κ0有一定容忍度。

### 7. 优点

- **方法创新**：将RL策略改进建模为密度传输，天然契合流匹配模型的数学结构，避免了有偏梯度、蒸馏或时间反向传播。
- **梯度稳定性**：通过期望目标预测在流形上计算SVGD更新，产生平衡且稳定的梯度信号。
- **保留多模态性**：SVGD的排斥项防止模式坍缩，且无需压缩为少步模型。
- **算法通用性**：适用于多种任务类型（密集/稀疏奖励、状态/视觉输入）。
- **计算效率**：仅需4个粒子即可获得良好性能，减轻了计算负担。

### 8. 不足与局限

- **计算开销**：需要并行模拟和梯度计算多个SVGD粒子（K≥4），相较于单步更新方法有一定额外成本。尽管可接受，但仍可能对实时机器人RL构成挑战。
- **实验覆盖**：未在真实机器人上部署，仅在仿真器中验证。论文指出其适合仿真到真实的迁移流程，但尚未提供真实性证据。
- **基线限制**：部分基线（如ReinFlow）在原论文中表现更好，但此处可能因实现细节差异而表现不佳；QAM在机器人任务中调参困难导致未比较，可能影响全面性。
- **算法稳定性**：虽然梯度更均衡，但未提供收敛性理论证明，仅给出实验观察。
- **消融分析不足**：未深入研究不同核函数带宽设对性能的影响，仅使用中位数距离作为经验设置。
- **潜在偏差**：预训练策略相同，但不同基线的实现细节（如学习率、更新步数）可能未完全公平——例如FPO++使用了额外的截断参数搜索（找到0.01最佳），而RLDT也进行了少量调参，但论文声称尽量保持一致。

（完）
